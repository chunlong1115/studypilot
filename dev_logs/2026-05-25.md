# 开发日志 - 2026-05-25

## 完成事项

### 第 1 步：项目骨架搭建 ✅ 完成
- 确定项目需求与功能模块（6大模块）
- 确定技术栈选型（Flask + MySQL + DeepSeek API + Bootstrap 5）
- 创建完整项目目录结构
- 编写 `requirements.txt`（18个依赖包）并全部安装成功
- 编写 `config.py`（开发/生产/测试三套配置）+ `.env` + `.env.example`
- 编写 `studypilot/__init__.py`（app 工厂函数）
- 编写 `studypilot/extensions.py`（db, migrate, login_manager, mail）
- 编写 `run.py` 应用入口
- 编写 `base.html` + `index.html` + `style.css` + `main.js`
- 创建 `docs/` 下 5 个标准文档 + `CLAUDE.md` + `dev_logs/`

### 第 2 步：数据库 + 用户认证 ✅ 完成
- 连接 MySQL，创建 `studypilot` 数据库（utf8mb4）
- 编写 `models.py`（10 张表 + user_loader）
- Flask-Migrate 初始化 + 首次迁移 + 应用成功
- 编写 `utils.py`（文件上传、令牌生成、邮件发送）
- 编写 `auth.py` 蓝图：注册 / 登录 / 登出 / 邮箱验证 / 忘记密码 / 重置密码
- 编写 4 个认证模板页面
- 编写 `user.py` 蓝图骨架 + `dashboard.html` 仪表盘
- 恢复 `login_manager`，导航栏支持已登录/未登录状态切换
- 端到端测试全部通过：
  - 着陆页 / 登录页 / 注册页 → HTTP 200
  - 注册 → 创建用户 + 邮件令牌 → HTTP 302
  - 邮箱验证 → 用户激活 → HTTP 302
  - 登录 → 仪表盘 → HTTP 200
  - 仪表盘保护 → 未登录用户重定向 → HTTP 302
  - 登出 → HTTP 302
  - 忘记密码 → 生成重置令牌 → HTTP 302

## 遇到的问题及解决方案

1. **清华 PyPI 镜像 403** → 切换官方源
2. **Flask-Login 缺少 user_loader** → 在 models.py 中定义 `@login_manager.user_loader`
3. **url_for 找不到 auth 路由** → 先创建 auth 蓝图骨架再跑 `flask db init`

### 第 3 步：用户中心 ✅ 完成
- 编写 `user.py` 蓝图：个人信息查看/修改、头像上传、学习偏好设置
- 编写 `templates/user/profile.html`：头像展示/上传、用户名修改、邮箱展示、注册时间
- 编写 `templates/user/preferences.html`：学科下拉、目标下拉、难度单选、时长滑块
- 端到端测试全部通过：
  - 个人中心 GET/POST → HTTP 200 / 302
  - 用户名修改 → 数据库更新
  - 学习偏好保存 → 各字段正确写入数据库

## 待办事项
- [ ] 第 4 步：仪表盘动态统计
- [ ] 第 5 步：学习资料管理

## 下一步
第 4 步：仪表盘 + 第 5 步：学习资料管理
